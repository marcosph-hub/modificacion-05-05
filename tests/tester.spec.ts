import 'mocha';
import { expect } from 'chai';
//import { newFunction } from '../src/index';

describe('prueba',() => {
    it ('prueba',() => {
        //expect(newFunction()).to.be.eql("hola")
    });
});

it ('',() => {})
